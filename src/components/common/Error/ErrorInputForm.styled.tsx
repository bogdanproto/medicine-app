import styled from 'styled-components';

export const ErrorInputForm = styled.p`
  position: absolute;

  padding-left: 6px;

  font-size: 12px;

  color: ${({ theme }) => theme.colorErrorsInput};
`;
